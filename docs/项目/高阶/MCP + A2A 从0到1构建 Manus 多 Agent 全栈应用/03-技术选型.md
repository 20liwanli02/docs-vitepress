# 第3章：技术选型

## 一、技术栈总览

```
┌─────────────────────────────────────────────────────────────────┐
│                      技术栈全景图                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  前端技术栈                      后端技术栈                      │
│  ┌─────────────────────┐       ┌─────────────────────┐         │
│  │ Next.js 16          │       │ Python 3.12         │         │
│  │ React 19            │       │ FastAPI             │         │
│  │ TypeScript          │       │ SQLAlchemy          │         │
│  │ Tailwind CSS        │       │ Pydantic            │         │
│  │ Radix UI            │       │ asyncio             │         │
│  │ noVNC               │       │ Playwright          │         │
│  └─────────────────────┘       └─────────────────────┘         │
│                                                                 │
│  存储技术栈                      基础设施                        │
│  ┌─────────────────────┐       ┌─────────────────────┐         │
│  │ PostgreSQL          │       │ Docker              │         │
│  │ Redis               │       │ Nginx               │         │
│  │ 腾讯云 COS          │       │ Supervisord         │         │
│  └─────────────────────┘       └─────────────────────┘         │
│                                                                 │
│  AI 技术栈                       协议支持                        │
│  ┌─────────────────────┐       ┌─────────────────────┐         │
│  │ OpenAI API          │       │ MCP 协议            │         │
│  │ DeepSeek API        │       │ A2A 协议            │         │
│  │ LangChain           │       │ SSE 流式响应        │         │
│  └─────────────────────┘       └─────────────────────┘         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、后端技术选型

### 2.1 核心框架

#### FastAPI

| 维度 | 说明 |
|-----|------|
| **简介** | 现代、快速的 Web 框架 |
| **优势** | 高性能、原生异步、自动文档、类型提示 |
| **适用场景** | API 服务、异步任务处理 |
| **项目使用** | 所有 API 接口开发 |

```python
from fastapi import FastAPI, Depends
from pydantic import BaseModel

app = FastAPI()

class ChatRequest(BaseModel):
    message: str

@app.post("/api/chat")
async def chat(request: ChatRequest):
    return {"response": "..."}
```

**选型理由**：
- ✅ 原生支持异步，适合 Agent 长时间运行场景
- ✅ 自动生成 API 文档，开发效率高
- ✅ Pydantic 集成，数据验证方便
- ✅ 性能优秀，适合高并发场景

#### SQLAlchemy

| 维度 | 说明 |
|-----|------|
| **简介** | Python ORM 框架 |
| **优势** | 功能强大、支持异步、灵活 |
| **适用场景** | 数据库操作 |
| **项目使用** | 所有数据库操作 |

```python
from sqlalchemy.ext.asyncio import AsyncSession, create_async_engine
from sqlalchemy.orm import sessionmaker

engine = create_async_engine("postgresql+asyncpg://...")
async_session = sessionmaker(engine, class_=AsyncSession)
```

**选型理由**：
- ✅ 支持异步操作，与 FastAPI 配合良好
- ✅ 成熟稳定，生态丰富
- ✅ 支持复杂查询和关系映射

### 2.2 AI 相关

#### OpenAI SDK

| 维度 | 说明 |
|-----|------|
| **简介** | OpenAI 官方 Python SDK |
| **优势** | 官方支持、功能完整 |
| **适用场景** | LLM 调用 |
| **项目使用** | 所有 AI 模型调用 |

```python
from openai import AsyncOpenAI

client = AsyncOpenAI(api_key="...")
response = await client.chat.completions.create(
    model="gpt-4",
    messages=[{"role": "user", "content": "..."}],
    tools=[...],
)
```

**选型理由**：
- ✅ 官方维护，稳定可靠
- ✅ 支持 Function Calling
- ✅ 兼容 OpenAI 格式的其他模型（DeepSeek 等）

#### MCP SDK

| 维度 | 说明 |
|-----|------|
| **简介** | Model Context Protocol SDK |
| **优势** | 官方支持、协议完整 |
| **适用场景** | 外部工具集成 |
| **项目使用** | MCP 工具调用 |

```python
from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client

async with stdio_client(server_params) as (read, write):
    async with ClientSession(read, write) as session:
        await session.initialize()
        tools = await session.list_tools()
```

**选型理由**：
- ✅ 官方维护，协议标准
- ✅ 支持多种传输协议（stdio、SSE、HTTP）
- ✅ 生态丰富，工具多

### 2.3 浏览器自动化

#### Playwright

| 维度 | 说明 |
|-----|------|
| **简介** | 现代浏览器自动化框架 |
| **优势** | 跨浏览器、异步支持、功能强大 |
| **适用场景** | 网页自动化 |
| **项目使用** | 浏览器工具实现 |

```python
from playwright.async_api import async_playwright

async with async_playwright() as p:
    browser = await p.chromium.launch()
    page = await browser.new_page()
    await page.goto("https://example.com")
    await page.click("button")
```

**选型理由**：
- ✅ 原生异步支持
- ✅ 比 Selenium 更现代、更快
- ✅ 自动等待、智能定位

---

## 三、前端技术选型

### 3.1 核心框架

#### Next.js

| 维度 | 说明 |
|-----|------|
| **简介** | React 全栈框架 |
| **优势** | SSR/SSG、路由、API 路由 |
| **适用场景** | Web 应用开发 |
| **项目使用** | 前端应用框架 |

**选型理由**：
- ✅ React 生态，组件丰富
- ✅ 服务端渲染，SEO 友好
- ✅ 开发体验好，热更新快

#### React 19

| 维度 | 说明 |
|-----|------|
| **简介** | UI 库 |
| **优势** | 组件化、声明式、生态丰富 |
| **适用场景** | UI 开发 |
| **项目使用** | 所有 UI 组件 |

**选型理由**：
- ✅ 生态最成熟
- ✅ 组件库丰富
- ✅ 团队熟悉

### 3.2 UI 组件

#### Radix UI + Tailwind CSS

| 技术 | 说明 |
|-----|------|
| Radix UI | 无样式组件库，可访问性好 |
| Tailwind CSS | 原子化 CSS 框架 |

**选型理由**：
- ✅ Radix UI 可访问性好，组件完整
- ✅ Tailwind CSS 开发效率高
- ✅ 组合使用灵活度高

### 3.3 特殊组件

#### noVNC

| 维度 | 说明 |
|-----|------|
| **简介** | HTML5 VNC 客户端 |
| **优势** | 纯前端、无需插件 |
| **适用场景** | 远程桌面 |
| **项目使用** | 浏览器实时预览 |

**选型理由**：
- ✅ 纯前端实现
- ✅ 支持 WebSocket
- ✅ 开源免费

---

## 四、存储技术选型

### 4.1 数据库

#### PostgreSQL

| 维度 | 说明 |
|-----|------|
| **简介** | 开源关系型数据库 |
| **优势** | 功能强大、扩展性好、支持 JSON |
| **适用场景** | 数据持久化 |
| **项目使用** | 会话、文件、事件存储 |

**选型理由**：
- ✅ 功能强大，支持复杂查询
- ✅ 支持 JSON 类型，灵活
- ✅ 开源免费，社区活跃

### 4.2 缓存

#### Redis

| 维度 | 说明 |
|-----|------|
| **简介** | 内存数据库 |
| **优势** | 高性能、数据结构丰富 |
| **适用场景** | 缓存、消息队列 |
| **项目使用** | 会话状态、消息队列、Agent 记忆 |

**选型理由**：
- ✅ 性能极高
- ✅ 支持多种数据结构
- ✅ 支持消息队列（Stream）

### 4.3 对象存储

#### 腾讯云 COS

| 维度 | 说明 |
|-----|------|
| **简介** | 云对象存储服务 |
| **优势** | 稳定、便宜、CDN 加速 |
| **适用场景** | 文件存储 |
| **项目使用** | 用户上传文件存储 |

**选型理由**：
- ✅ 国内访问速度快
- ✅ 价格便宜
- ✅ SDK 完善

---

## 五、基础设施选型

### 5.1 容器化

#### Docker

| 维度 | 说明 |
|-----|------|
| **简介** | 容器化平台 |
| **优势** | 环境一致、部署简单 |
| **适用场景** | 应用部署 |
| **项目使用** | 所有服务容器化 |

**选型理由**：
- ✅ 环境隔离
- ✅ 部署简单
- ✅ 生态完善

### 5.2 反向代理

#### Nginx

| 维度 | 说明 |
|-----|------|
| **简介** | 高性能 Web 服务器 |
| **优势** | 高并发、反向代理、负载均衡 |
| **适用场景** | Web 服务器 |
| **项目使用** | 统一入口、反向代理 |

**选型理由**：
- ✅ 性能极高
- ✅ 配置简单
- ✅ 支持 WebSocket

---

## 六、技术对比

### 6.1 后端框架对比

| 对比项 | FastAPI | Flask | Django |
|-------|---------|-------|--------|
| 性能 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| 异步支持 | ✅ 原生 | ❌ 需扩展 | ⚠️ 部分 |
| 学习曲线 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| 类型提示 | ✅ 强制 | ❌ 可选 | ❌ 可选 |
| API 文档 | ✅ 自动 | ❌ 需插件 | ❌ 需插件 |
| 适合场景 | API 服务 | 小型应用 | 全栈应用 |

**结论**：FastAPI 最适合 AI Agent 这种需要异步、高性能 API 的场景。

### 6.2 浏览器自动化对比

| 对比项 | Playwright | Selenium | Puppeteer |
|-------|-----------|----------|-----------|
| 异步支持 | ✅ 原生 | ❌ 需封装 | ✅ 原生 |
| 浏览器支持 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 性能 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 功能完整性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 学习曲线 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |

**结论**：Playwright 异步支持好、功能完整，最适合本项目。

### 6.3 前端框架对比

| 对比项 | Next.js | Nuxt.js | SvelteKit |
|-------|---------|---------|-----------|
| 生态 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| SSR | ✅ | ✅ | ✅ |
| 学习曲线 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 组件库 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| 企业应用 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |

**结论**：Next.js 生态最成熟，组件库丰富，适合企业级应用。

---

## 七、技术演进路线

### 7.1 当前版本

```
v1.0
├── FastAPI + SQLAlchemy
├── Next.js + React
├── PostgreSQL + Redis
├── Docker + Nginx
└── MCP + A2A 协议
```

### 7.2 未来规划

```
v2.0 (规划中)
├── 支持 RAG 知识库
├── 支持多模态（图像、语音）
├── 支持 Agent 工作流编排
├── 支持 Kubernetes 部署
└── 支持更多 LLM 模型
```

---

## 八、下一步

- 🏗️ 理解架构设计：[第4章 架构设计](./04-架构设计.md)
- 🤖 学习 Agent 原理：[第5章 Agent核心原理](./05-Agent核心原理.md)

---

**上一章**：[快速开始](./02-快速开始.md) | **下一章**：[架构设计](./04-架构设计.md)
